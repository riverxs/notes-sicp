(define f 
	(let ((c 0))
		(lambda (x) 
			(cond ((= c 0) 
				  	(begin (set! c (+ c 1)) x))
				  ((> c 0) 
				  	0)))))


; 求值
(+ (f 0) (f 1))
; 如果求值从左到右会返回0

; 求值
(+ (f 0) (f 1))
; 如果求值从右到左会返回1

; 从解释器结果上看先返回1，故求值是从右到左
